import git
import shutil
import os

# Set these variables
local_repo_path = "/Users/vishwadish/Desktop/DS Course/Project-1"  # Change to your local Git repo path
file_to_add = "/mnt/data/train.csv"
destination_path = os.path.join(local_repo_path, "train.csv")

# Step 1: Copy the file to your repo
shutil.copy(file_to_add, destination_path)

# Step 2: Initialize repo object
repo = git.Repo(local_repo_path)

# Step 3: Stage the file
repo.git.add("train.csv")

# Step 4: Commit the change
repo.index.commit("Add train.csv file")

# Step 5: Push to remote
origin = repo.remote(name='origin')
origin.push()

print("train.csv added and pushed to GitHub!")
